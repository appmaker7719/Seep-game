# Seep Game — GitHub Ready

Upload this project to a GitHub repository. Then open **Actions** and run
**Build Seep APK**. The finished `app-debug.apk` will be available as an
artifact named `SeepGame-debug-apk`.

Modes: 2 Player and 4 Player.
